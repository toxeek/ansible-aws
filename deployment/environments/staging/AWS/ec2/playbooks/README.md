##############################################################

An example of playbook execution would be:

$ ansible-playbook --private-key ~/.ssh/MY-key-pair-ireland.pem -e "env=staging" -vvvv create_instance.yml

##############################################################
